namespace Atlas.LicenseTool;

public static class DevDefaults
{
    // Canonical dev marker fields (never used for production licenses)
    public const string DevIssuer = "dev";
    public const string DevCustomer = "Dev";
}